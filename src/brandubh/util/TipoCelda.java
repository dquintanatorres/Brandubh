/**
 * Clases de utilidad empleadas por las clases del resto de paquetes.
 * <p>
 * Contiene tipos enumerados, registros y clases de utilidad reutilizados en el resto de paquetes.
 * 
 * @author Diego Quintana Torres
 * @version 1.0
 * @since 1.0
 */
package brandubh.util;

/**
 * Tipo de celda de las tres posibles (trono, provincia o normal) en un tablero de Brandubh.
 * 
 * @author Diego Quintana Torres
 * @version 1.0
 * @since 1.0 
 */
public enum TipoCelda {

	/**
	 * Celda de tipo trono.
	 */
	TRONO,

	/**
	 * Celda de tipo provincia.
	 */
	PROVINCIA,

	/**
	 * Celda de tipo normal.
	 */
	NORMAL;
}
